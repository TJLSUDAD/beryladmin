import { Layout } from '@/components/layout/Layout';
import { LensContainer } from '@/components/lens/LensContainer';

function App() {
  return (
    <Layout>
      <LensContainer />
    </Layout>
  );
}

export default App;